# requirements.txt — Monitor de Ajuste Presupuestario (MAP) v2.0

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.30

# HTTP client (scraping + BCRA API)
requests>=2.31.0
httpx>=0.27.0

# Browser automation (BORA scraping)
playwright>=1.44.0
# Instalar browsers: playwright install chromium

# PDF parsing (tablas de anexos)
camelot-py[cv]>=0.11.0
pdfplumber>=0.11.0
ghostscript  # requerido por camelot

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Visualization
plotly>=5.22.0

# Pydantic
pydantic>=2.7.0